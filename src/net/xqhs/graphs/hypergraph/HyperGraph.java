package net.xqhs.graphs.hypergraph;

import net.xqhs.graphs.graph.SimpleGraph;

/**
 * Represents a hyper graph, that can contain nodes which are hyper graphs ({@link HyperNode} instances).
 * <p>
 * It relies on the {@link SimpleGraph} implementation.
 * <p>
 * Currently it does not differ from it.
 * 
 * @author Andrei Olaru
 */
public class HyperGraph extends SimpleGraph
{
	// nothing to extend, for the moment.
}
